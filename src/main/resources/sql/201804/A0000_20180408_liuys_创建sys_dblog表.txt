CREATE TABLE SYS_DBLOG
(
  SID       VARCHAR(6)                    NOT NULL COMMENT 'ID',
  STATUS    int(2) COMMENT '状态',
  RUN_TIME  timestamp NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '运行时间',
  PRIMARY KEY (`sid`)
)
